The executable file in this folder should be used for the first scanning and upload to the FTP server (For the scan process). 
You can use this executable, scan with yourself for the first time to find a bot or you can use servers, devices, or 
anything is run Linux that you have been hacked to scan it. It's up to you.
	 
To run the payload you need to run with root access!!!
